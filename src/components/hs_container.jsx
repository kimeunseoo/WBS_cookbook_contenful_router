import { useParams } from "react-router-dom";

const HS_contianer = ({foodState}) => {
  const foodParam = useParams();
  // return (
  //   <h1>{foodState.length>0 ? foodState.filter(item => item.fields.name === foodParam):""}</h1>
  // );

  
}

export default HS_contianer;